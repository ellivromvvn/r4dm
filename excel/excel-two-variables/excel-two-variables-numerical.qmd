---
title: "Two Variables: Quantitative"
author: Orville D. Hombrebueno
date: today
format: 
  docx:
    reference-doc: excel-report-template.docx
editor: visual
toc: true
cache: true
execute: 
  echo: false
---

# The Data

```{r}
#| label: preliminaries
#| warning: false
#| message: false

# install packages: install.packages("package")
# load packages
library(tidyverse) 
library(knitr)
library(gtsummary)
library(ggstatsplot)
library(downlit)
library(broom)

# load data
data(mtcars)
```

## Motor Trend Car Road Tests

The data was extracted from the 1974 Motor Trend US magazine, and comprises fuel consumption and 10 aspects of automobile design and performance for 32 automobiles (1973–74 models).

A data frame with 32 observations on 11 (numeric) variables.

-   `mpg` Miles/(US) gallon

-   `cyl` Number of cylinders

-   `disp` Displacement (cu.in.)

-   `hp` Gross horsepower

-   `drat` Rear axle ratio

-   `wt` Weight (1000 lbs)

-   `qsec` 1/4 mile time

-   `vs` Engine (0 = V-shaped, 1 = straight)

-   `am` Transmission (0 = automatic, 1 = manual)

-   `gear` Number of forward gears

-   `carb` Number of carburetors

The first 6 rows of the data set are shown in @tbl-mtcars

```{r}
#| label: tbl-mtcars
#| warning: false
#| message: false
#| error: false
#| tbl-cap: "The first 6 observations of the data set."

mtcars |> # call the data
  head() |> # get the first six rows of the data
  kable() # create a table
```

## Descriptive Statistics

The mean, standard deviation, minimum, and maximum of `hp` and `mpg` in the `mtcars` data set are shown in @tbl-summary. There are a total of 32 observations.

```{r}
#| label: tbl-summary
#| tbl-cap: "Summary statistics of the data set."
#| message: false

mtcars |> # call the data
  tbl_summary( # create a table
    include = c(mpg, hp), # select variables
    statistic = all_continuous() ~ "{mean} ± {sd} ({min} - {max})", # descriptive statistics
    digits = all_continuous() ~ 2 # no. of digits (rounding)
  )
```

A visualization of miles/(US) gallon and gross horsepower is shown in @fig-viz.

![Scatterplot of `mpg` and `hp`.](images/viz-num.png){#fig-viz}

# Hypothesis Testing

## Research Question

Is there a significant relationship between gross horsepower and miles/(US) gallon?

## Hypothesis and Test Statistics

-   $H_0:\rho=0$; there is no linear relationship between gross horsepower and miles/(US) gallon.

-   $H_a:\rho\ne0$; there is a linear relationship between gross horsepower and miles/(US) gallon.

-   Significance level, $\alpha=5\%$.

-   **Pearson correlation**

-   $n>30$; large sample.

## Results and Interpretation

The test statistics is shown on @tbl-stats.

```{r}
#| label: tbl-stats
#| tbl-cap: Test statistics.

cor.test(mtcars$mpg, mtcars$hp, method = "pearson") |>
  tidy() |>
  select(
    estimate,
    statistic,
    p.value,
    method
  ) |>
  mutate(across(where(is.numeric), ~ round(.x, 2))) |>
  kable()
```

Based on the *p*-value being less than 5% (*p*-value = .001), we reject the null hypothesis. There is strong evidence of a linear relationship between gross horsepower and miles/(US) gallon. The correlation ($\rho=-0.78$) suggests a moderate negative relationship between the two variables. Increased gross horsepower is moderately associated with decreased miles/(US) gallon.